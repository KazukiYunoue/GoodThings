<%= form_for(@good_thing) do |f| %>
  <% if @good_thing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@good_thing.errors.count, "error") %> prohibited this good_thing from being saved:</h2>

      <ul>
      <% @good_thing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :count %><br />
    <%= f.text_field :count %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
